import Link from "next/link";
import { requireDatabase, requireEditor } from "@/lib/admin";

export const dynamic = "force-dynamic";
export default async function PropFirmsAdminPage() { await requireEditor(); const db = requireDatabase(); const firms = await db.propFirm.findMany({ orderBy: { updatedAt: "desc" }, take: 100 }); return <main className="admin-shell"><header className="admin-header"><div><p className="eyebrow">COMPARISON DATA</p><h1>Prop firms</h1></div><Link href="/admin/prop-firms/new" className="button button-primary">New Prop Firm</Link></header><nav className="admin-nav"><Link href="/admin">Overview</Link><Link href="/admin/articles">Articles</Link><Link href="/admin/audits">Audits</Link><Link href="/admin/prop-firms">Prop firms</Link></nav><div className="admin-table"><table><thead><tr><th>Name</th><th>Assessment</th><th>Last reviewed</th><th /></tr></thead><tbody>{firms.map((firm) => <tr key={firm.id}><td>{firm.name}<small>{firm.platform ?? "No platform recorded"}</small></td><td>{firm.assessment ?? "No assessment"}</td><td>{firm.lastReviewedAt?.toISOString().slice(0, 10) ?? "Missing"}</td><td><Link className="text-link" href={`/admin/prop-firms/${firm.id}`}>Edit</Link></td></tr>)}</tbody></table></div></main>; }
