import Link from "next/link";
import { requireDatabase, requireEditor } from "@/lib/admin";

export const dynamic = "force-dynamic";
export default async function ArticlesAdminPage() { await requireEditor(); const db = requireDatabase(); const articles = await db.article.findMany({ orderBy: { updatedAt: "desc" }, take: 100 }); return <main className="admin-shell"><header className="admin-header"><div><p className="eyebrow">CONTENT</p><h1>Articles</h1></div><Link href="/admin/articles/new" className="button button-primary">New article</Link></header><nav className="admin-nav"><Link href="/admin">Overview</Link><Link href="/admin/articles">Articles</Link><Link href="/admin/audits">Audits</Link><Link href="/admin/prop-firms">Prop firms</Link></nav><div className="admin-table"><table><thead><tr><th>Title</th><th>Status</th><th>Updated</th><th /></tr></thead><tbody>{articles.map((article) => <tr key={article.id}><td>{article.title}<small>/{article.slug}</small></td><td>{article.status}</td><td>{article.updatedAt.toISOString().slice(0, 10)}</td><td><Link className="text-link" href={`/admin/articles/${article.id}`}>Edit</Link></td></tr>)}</tbody></table></div></main>; }
